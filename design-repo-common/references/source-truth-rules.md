# Source truth rules

- Do not treat runtime state, caches, generated screenshots, or tool exports as source truth.
- Merge generated insights back through a source-authoring skill.
- Mark assumptions explicitly.
- Stop when product semantics are missing.
